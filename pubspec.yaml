name: flutter_login
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.2.6 <4.0.0'

dependencies:
  login_core:
    path: packages/login_core
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  meta: ^1.10.0
  flutter_bloc: ^8.1.4
  formz: ^0.7.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

